export default function saveReducer(state = initialState, action) {
    switch (action.type) {
        case 'DO_SEARCH':

            return 

        case 'SHOW_RESULTS':
           
            return

        case 'SHOW_ITEM':
            return 
            
        case 'ADD_ITEM':
            return

        default:
            return state
    }
}